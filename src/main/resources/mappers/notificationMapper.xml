<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rateye.mapper.NotificationMapper">

<!--    알림 추가-->
    <insert id="insertNotification">
        INSERT INTO notification (not_id ,mem_id, target_mem_id, not_type, not_content_id, not_message, not_url, not_datetime) VALUES
                        (null, #{mem_id}, #{target_mem_id}, #{not_type}, #{not_content_id}, #{not_message}, #{not_url}, not_datetime)
    </insert>
</mapper>
